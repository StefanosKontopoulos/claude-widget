import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Claude Widget")
                .font(.largeTitle)
                .padding()
            Text("App Groups test will appear here in Plan 01-04")
                .font(.caption)
                .foregroundColor(.secondary)
        }
    }
}
