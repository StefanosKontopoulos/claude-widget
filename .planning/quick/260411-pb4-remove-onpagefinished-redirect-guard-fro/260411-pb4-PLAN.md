---
phase: quick-260411-pb4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt
autonomous: true
requirements: [QUICK-260411-pb4]
must_haves:
  truths:
    - "The onPageFinished override no longer bounces the WebView back to /login"
    - "The Google-hide JS is still injected on every claude.ai page load"
    - "super.onPageFinished and the claude.ai URL guard are preserved"
  artifacts:
    - path: "android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt"
      provides: "LoginActivity with redirect guard removed"
      contains: "override fun onPageFinished"
  key_links:
    - from: "onPageFinished"
      to: "HIDE_GOOGLE_JS"
      via: "evaluateJavascript call inside url.contains(\"claude.ai\") block"
      pattern: "evaluateJavascript.*HIDE_GOOGLE_JS"
---

<objective>
Remove the redirect guard block from `onPageFinished` in LoginActivity that causes an infinite redirect loop on claude.ai (a React SPA whose client-side navigation away from `/login` falsely triggers a bounce back to `/login`).

Purpose: Stop the WebView from looping and settling on the marketing landing page instead of completing the login flow.
Output: LoginActivity.kt with the guard removed; all other logic intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redirect guard from onPageFinished</name>
  <files>android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt</files>
  <action>
    Replace the entire `onPageFinished` override (lines 114-129) with the stripped-down version below.

    Remove ONLY the redirect guard block — the `isLoginPage` local variable, the `if (!isLoginPage && capturedOrgId == null && !loginHandled)` block, and its `return` statement. Keep everything else.

    The resulting method must be exactly:

    ```kotlin
    override fun onPageFinished(view: WebView?, url: String?) {
        super.onPageFinished(view, url)
        if (url != null && url.contains("claude.ai")) {
            view?.evaluateJavascript(HIDE_GOOGLE_JS, null)
            Log.d(TAG, "Injected Google-hide JS on $url")
        }
    }
    ```

    Do NOT touch any other part of the file — the companion object, `shouldInterceptRequest`, `onCreate`, `completeLogin`, or the cookie/cache clearing logic.

    Note: `capturedOrgId` and `loginHandled` fields remain in the class; they are still used by `shouldInterceptRequest` and `completeLogin`. Do not remove them.
  </action>
  <verify>
    <automated>grep -n "isLoginPage\|bouncing back\|loadUrl(LOGIN_URL)" "android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt" | wc -l</automated>
  </verify>
  <done>
    The grep above returns 0 (no remaining redirect guard lines). The file still contains `evaluateJavascript(HIDE_GOOGLE_JS` and `super.onPageFinished`.
  </done>
</task>

</tasks>

<verification>
After the edit:
- `grep -n "isLoginPage" android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt` returns no output
- `grep -n "evaluateJavascript" android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt` returns exactly one match inside `onPageFinished`
- `grep -n "super.onPageFinished" android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt` returns one match
</verification>

<success_criteria>
LoginActivity.kt compiles (no syntax errors introduced) and `onPageFinished` contains no redirect guard — only the `super` call and the JS injection block.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-pb4-remove-onpagefinished-redirect-guard-fro/260411-pb4-SUMMARY.md`
</output>
