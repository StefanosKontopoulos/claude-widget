---
phase: quick-260411-q6y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt
autonomous: true
requirements:
  - hide-or-divider
must_haves:
  truths:
    - "The OR divider text is no longer visible on the login page after Google button is hidden"
  artifacts:
    - path: "android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt"
      provides: "Updated HIDE_GOOGLE_JS with OR-divider hiding loop"
      contains: "dividers[j].style.setProperty"
  key_links:
    - from: "HIDE_GOOGLE_JS constant"
      to: "hideGoogle() function body"
      via: "second querySelectorAll loop targeting p, span, div"
      pattern: "dividers.*querySelectorAll"
---

<objective>
Add a second loop inside the existing `hideGoogle()` function in `HIDE_GOOGLE_JS` that hides any `p`, `span`, or `div` element whose trimmed `innerText`/`textContent` is exactly "OR", "or", or "Or".

Purpose: The Google sign-in button was hidden in a prior task. This leaves an orphaned "OR" divider visible on the login page. Hiding it requires scanning text-only elements since the divider is not a button or link.
Output: Updated `HIDE_GOOGLE_JS` constant in LoginActivity.kt. No other changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OR-divider hiding loop inside hideGoogle()</name>
  <files>android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt</files>
  <action>
In the `HIDE_GOOGLE_JS` constant (lines 23–41), insert the following block immediately after the closing brace of the existing `for` loop (after line 34 `}`), still inside the `hideGoogle()` function body, before the closing `}` of `hideGoogle`:

```javascript
                  var dividers = document.querySelectorAll('p, span, div');
                  for (var j = 0; j < dividers.length; j++) {
                    var txt = (dividers[j].innerText || dividers[j].textContent || '').trim();
                    if (txt === 'OR' || txt === 'or' || txt === 'Or') {
                      dividers[j].style.setProperty('display', 'none', 'important');
                    }
                  }
```

The result should be that `hideGoogle()` first hides all Google-labelled buttons/links, then hides all p/span/div elements whose full visible text is exactly "OR" (any casing).

Do NOT change any other part of the file — no imports, no other functions, no indentation outside the inserted block.
  </action>
  <verify>
    <automated>grep -n "dividers\[j\]" "android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt"</automated>
  </verify>
  <done>The grep returns at least one match showing `dividers[j].style.setProperty` inside LoginActivity.kt, confirming the loop was inserted. The file compiles without errors (`./gradlew :app:compileDebugKotlin` exits 0).</done>
</task>

</tasks>

<verification>
1. `grep -n "dividers\[j\]" android/app/src/main/java/com/claudewidget/auth/LoginActivity.kt` returns matches.
2. `./gradlew :app:compileDebugKotlin` exits 0 — no Kotlin compilation errors.
3. No other lines in the file were modified (diff shows only the inserted JS block).
</verification>

<success_criteria>
LoginActivity.kt contains the OR-divider loop inside `hideGoogle()`, the file compiles cleanly, and no other code was changed.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-q6y-hide-orphaned-or-divider-in-hide-google-/260411-q6y-SUMMARY.md`
</output>
