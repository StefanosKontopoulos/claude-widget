---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/build.gradle.kts
  - android/settings.gradle.kts
  - android/gradle/libs.versions.toml
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - android/gradle.properties
  - android/gradlew
  - android/gradlew.bat
  - android/gradle/wrapper/gradle-wrapper.properties
autonomous: true
requirements:
  - SETUP-01
  - SETUP-04

must_haves:
  truths:
    - "`./gradlew assembleDebug` exits 0 from the android/ directory"
    - "All six dependency groups (glance-appwidget, work-runtime-ktx, okhttp, kotlinx-serialization-json, datastore-preferences, security-crypto) are listed in libs.versions.toml"
    - "applicationId is exactly `com.claudewidget`, minSdk is 26, targetSdk is 35"
    - "kotlinx-serialization plugin is applied so @Serializable compiles without error"
  artifacts:
    - path: "android/app/build.gradle.kts"
      provides: "App-level Gradle config with all dependencies and plugin applications"
      contains: "com.claudewidget"
    - path: "android/gradle/libs.versions.toml"
      provides: "Version catalog with all six dependency entries"
      contains: "glance-appwidget"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Minimal manifest with package declaration"
      contains: "com.claudewidget"
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "android/gradle/libs.versions.toml"
      via: "version catalog alias references (libs.androidx.glance etc.)"
      pattern: "libs\\.androidx\\.glance"
    - from: "android/build.gradle.kts"
      to: "kotlinx-serialization plugin"
      via: "plugins block"
      pattern: "kotlinx.serialization"
---

<objective>
Create the Android Gradle project scaffold with all required dependencies resolved and a clean debug build.

Purpose: This is the prerequisite for all Android work. Nothing can be built until the project compiles and all libraries are on the classpath.
Output: A Gradle KTS project in `android/` with Kotlin 2.x, AGP 9.x, and all six runtime dependencies (Glance, WorkManager, OkHttp, kotlinx-serialization, DataStore, security-crypto) declared in a version catalog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Android Gradle project structure and version catalog</name>
  <read_first>
    - .planning/research/STACK.md (exact versions for all six dependencies)
    - .planning/phases/01-foundation/01-RESEARCH.md (Gradle KTS config details, plugin versions)
  </read_first>
  <files>
    android/settings.gradle.kts
    android/build.gradle.kts
    android/gradle/libs.versions.toml
    android/gradle.properties
    android/gradle/wrapper/gradle-wrapper.properties
  </files>
  <action>
Create the top-level Android project files in the `android/` directory. The project does not yet exist — create all files from scratch.

**android/settings.gradle.kts:**
```kotlin
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "ClaudeWidget"
include(":app")
```

**android/build.gradle.kts:**
```kotlin
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.kotlin.android) apply false
    alias(libs.plugins.kotlin.serialization) apply false
}
```

**android/gradle/libs.versions.toml** (version catalog — exact versions from STACK.md):
```toml
[versions]
agp = "9.1.0"
kotlin = "2.3.20"
glance = "1.1.1"
workmanager = "2.11.1"
okhttp = "4.12.0"
kotlinx-serialization = "1.6.3"
datastore = "1.1.1"
security-crypto = "1.1.0"
coroutines = "1.8.1"
core-ktx = "1.16.0"
appcompat = "1.7.0"

[libraries]
androidx-glance-appwidget = { group = "androidx.glance", name = "glance-appwidget", version.ref = "glance" }
androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version.ref = "workmanager" }
okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version.ref = "okhttp" }
kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "kotlinx-serialization" }
androidx-datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore" }
androidx-security-crypto = { group = "androidx.security", name = "security-crypto", version.ref = "security-crypto" }
kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "core-ktx" }
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
```

**android/gradle.properties:**
```properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
```

**android/gradle/wrapper/gradle-wrapper.properties:**
```properties
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-9.3.1-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
```

Then run `gradle wrapper` inside the android/ directory (or copy gradlew/gradlew.bat from a template). If no local Gradle installation is available, download gradlew using:
```bash
cd android && gradle wrapper --gradle-version=9.3.1
```
  </action>
  <verify>
    <automated>grep -r "glance-appwidget" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/gradle/libs.versions.toml" && grep -r "security-crypto" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/gradle/libs.versions.toml" && grep -r "datastore-preferences" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/gradle/libs.versions.toml"</automated>
  </verify>
  <done>All six library entries (glance-appwidget, work-runtime-ktx, okhttp, kotlinx-serialization-json, datastore-preferences, security-crypto) present in libs.versions.toml. settings.gradle.kts, build.gradle.kts, and gradle.properties exist at android/ root.</done>
</task>

<task type="auto">
  <name>Task 2: Create app module build.gradle.kts and minimal source files, then verify clean build</name>
  <read_first>
    - android/gradle/libs.versions.toml (confirm alias names before referencing them)
    - android/settings.gradle.kts (confirm module name is `:app`)
    - .planning/phases/01-foundation/01-RESEARCH.md (file structure, package name, minSdk/targetSdk)
  </read_first>
  <files>
    android/app/build.gradle.kts
    android/app/src/main/AndroidManifest.xml
    android/app/src/main/java/com/claudewidget/ui/MainActivity.kt
    android/app/src/main/res/values/strings.xml
    android/app/src/main/res/layout/activity_main.xml
  </files>
  <action>
Create the `:app` module. All files go inside `android/app/`.

**android/app/build.gradle.kts:**
```kotlin
plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.serialization)
}

android {
    namespace = "com.claudewidget"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.claudewidget"
        minSdk = 26
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.appcompat)
    implementation(libs.androidx.glance.appwidget)
    implementation(libs.androidx.work.runtime.ktx)
    implementation(libs.okhttp)
    implementation(libs.kotlinx.serialization.json)
    implementation(libs.androidx.datastore.preferences)
    implementation(libs.androidx.security.crypto)
    implementation(libs.kotlinx.coroutines.android)
}
```

**android/app/src/main/AndroidManifest.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

**android/app/src/main/java/com/claudewidget/ui/MainActivity.kt** (minimal stub — data models come in plan 01-03):
```kotlin
package com.claudewidget.ui

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
```

**android/app/src/main/res/values/strings.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">Claude Widget</string>
</resources>
```

After creating all files, run the build from inside the `android/` directory:
```bash
cd "C:/Users/mkont/Desktop/Projects/Claude Widget/android" && ./gradlew assembleDebug --no-daemon 2>&1 | tail -20
```

If gradlew is not yet present, run `gradle wrapper --gradle-version=9.3.1` first (requires local Gradle installation). Alternatively on Windows, the wrapper can be bootstrapped by running `gradle.bat wrapper` in the android/ directory if Gradle is on PATH.

The build MUST exit 0. If it fails due to dependency resolution or version conflicts, resolve them before marking this task done.
  </action>
  <verify>
    <automated>grep "applicationId = \"com.claudewidget\"" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/app/build.gradle.kts" && grep "minSdk = 26" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/app/build.gradle.kts" && grep "targetSdk = 35" "C:/Users/mkont/Desktop/Projects/Claude Widget/android/app/build.gradle.kts"</automated>
  </verify>
  <done>android/app/build.gradle.kts contains applicationId "com.claudewidget", minSdk 26, targetSdk 35, and all six dependency aliases. `./gradlew assembleDebug` exits 0. An APK is produced at android/app/build/outputs/apk/debug/app-debug.apk.</done>
</task>

</tasks>

<verification>
From `android/` directory, run:
```bash
./gradlew assembleDebug --no-daemon
```
Build must exit 0 and produce `android/app/build/outputs/apk/debug/app-debug.apk`.

Confirm six dependencies resolve (no "Could not resolve" errors in output):
- androidx.glance:glance-appwidget:1.1.1
- androidx.work:work-runtime-ktx:2.11.1
- com.squareup.okhttp3:okhttp:4.12.0
- kotlinx-serialization-json:1.6.3
- androidx.datastore:datastore-preferences:1.1.1
- androidx.security:security-crypto:1.1.0
</verification>

<success_criteria>
1. `android/` directory exists with Gradle KTS structure (settings.gradle.kts, build.gradle.kts, gradle/libs.versions.toml, app/build.gradle.kts)
2. All six required dependencies declared in libs.versions.toml with exact versions from STACK.md
3. applicationId = "com.claudewidget", minSdk = 26, targetSdk = 35 in app/build.gradle.kts
4. kotlinx-serialization plugin applied (plugin alias present in both root and app build files)
5. `./gradlew assembleDebug` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at `@~/.claude/get-shit-done/templates/summary.md`.
</output>
